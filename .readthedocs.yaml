version: 2

build:
  os: 'ubuntu-22.04'
  tools:
    python: '3.12'

mkdocs:
  configuration: 'mkdocs.yml'

python:
  install:
    # Install the package itself so mkdocstrings can import flask_cors.
    - method: pip
      path: '.'
    - requirements: 'docs/requirements.txt'
